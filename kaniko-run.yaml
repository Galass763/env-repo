apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: build-and-deploy-kaniko-run-
  namespace: tekton-pipelines
spec:
  pipelineRef:
    name: build-and-deploy-kaniko
  params:
    - name: APP_REPO
      value: "https://github.com/Galass763/app-repo.git"
    - name: IMAGE_NAME
      value: "docker.io/<TON_USERNAME>/myapp:latest"
  workspaces:
    - name: shared-workspace
      persistentVolumeClaim:
        claimName: workspace-pvc
